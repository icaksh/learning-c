#include <stdio.h>
#include <stdlib.h>

// Declaring Global Variables
int characterAge; // 0
char characterName; // '\0'
float characterHeight; // 0
double characterSalary; // 0 
// pointer = null

int main(){
    // Declaring Local Variables
    int x,y,z;
    x = 90;
    y = x - 20;
    z = y + 30;
    printf("The value of z is %d",z);
    return 0;
}